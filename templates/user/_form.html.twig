{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="container">
    <div class="row">
        <div class="col text-center">
            <button class="btn btn-success ">{{ button_label|default("Créer un compte") }}</button>
        </div>
    </div>
</div>
{{ form_end(form) }}
